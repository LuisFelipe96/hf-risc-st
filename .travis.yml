sudo: false
language: cpp
compiler:
  - gcc
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-6
    - cppcheck
before_install:
  - python --version
  - pip install -U pip
install:
- "[ $CXX = g++ ] && export CXX=g++-6 || true"
- pip install gdown
script:
  - make

